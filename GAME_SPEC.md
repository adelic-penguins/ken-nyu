# 県乳 開発コンテキスト

## プロジェクト概要

「県乳」という対戦ゲームのMVPを開発している。

都道府県の地図上に一方のプレイヤーが「乳首」を**2箇所**配置し、
もう一方のプレイヤーが市区町村を選択して乳首の所在地を当てるゲーム。

乳首は2箇所配置する。同じ市区町村を2箇所目として選ぶことはできない（重複不可）。
配置した市区町村はピンクのハイライトを配置フェーズ中そのまま残す（消さない）。

現在は岩手県のみ対応する。

同じ端末をプレイヤー間で受け渡して遊ぶ想定。
オンライン対戦、ユーザー管理、バックエンド、データ永続化はMVPでは実装しない。

リロード時にゲーム状態が消えても問題ない。


## 最重要方針

MVPを短時間で完成させることを最優先する。

過剰な抽象化、汎用化、将来を見越した設計は行わない。

実装判断に迷った場合は、

「最も少ない実装でゲームを最後まで遊べるか」

を優先する。

コード品質は最低限保つが、
MVPに不要なリファクタリングや設計変更は行わない。


## 技術構成

- React
- TypeScript
- Vite
- MUI
- Mapbox GL JS
- GeoJSON

バックエンドなし。

SPAとして実装する。


## 地図データ

岩手県の市区町村境界GeoJSONを使用している。

現在使用しているファイル:

public/N03-21_03_210101.json

GeoJSONは以下のリポジトリ由来。

smartnews-smri/japan-topography

元データは国土交通省「国土数値情報 行政区域データ」。

市区町村の識別には現在 `N03_007` を使用している。

例:

N03_001: 岩手県
N03_004: 市区町村名
N03_007: 市区町村コード


## Mapbox実装

岩手県全体を固定表示している。

現在のbounds:

```ts
const IWATE_BOUNDS: mapboxgl.LngLatBoundsLike = [
    [140.65, 38.7],
    [142.15, 40.55],
];
```

ゲーム盤として使用するため、ユーザーによる地図操作は基本的に禁止する。

禁止対象:

- dragPan
- scrollZoom
- boxZoom
- doubleClickZoom
- touchZoomRotate
- keyboard
- dragRotate

ただし、市区町村ポリゴンのクリックイベントは使用する。


## 現在完成している機能

- Mapboxの表示
- 岩手県全体の表示
- GeoJSON読み込み
- 市区町村境界の描画
- 市区町村ポリゴンのクリック
- クリックした市区町村コードの取得
- React stateへの選択状態保存
- 選択中の市区町村のハイライト
- 選択した市区町村への乳首配置（1箇所のみ、実装中に2箇所へ変更予定）
- nippleMunicipalityへの市区町村コード保存
- phase state導入（`placement` / `handoff` / `guessing` / `result`）
- 端末受け渡しフェーズ（地図を隠す・受け渡し画面・準備OKボタン・探索フェーズへの遷移）


## 現在の主要state

selectedMunicipality:
現在選択している市区町村コード。

nippleMunicipalities:
乳首を配置した市区町村コードの配列（最大2つ）。
配置済みの市区町村はピンクのハイライトを維持したままにする。
同じ市区町村コードを2つ目として追加することはできない。

phase:
ゲーム進行状態（`placement` / `handoff` / `guessing` / `result`）。


## ゲームフロー

最終的なMVPのゲームフローは以下。

1. タイトル画面（phase: 'title'）
   - 「県乳」と「はじめる」ボタンを表示
   - 「はじめる」で配置フェーズへ

2. 乳首配置フェーズ
   - 岩手県の地図を表示
   - プレイヤー1が市区町村を選択
   - 選択中の市区町村をハイライト
   - 「ここに乳首を配置」を押す
   - nippleMunicipalitiesに市区町村コードを追加（配置済みはピンクのまま残す）
   - 既に配置済みの市区町村は選択・再配置できない
   - 合計2箇所配置したら次へ進める

3. 端末受け渡しフェーズ
   - 地図を隠す
   - 乳首の所在地を画面から完全に隠す
   - 「次のプレイヤーに端末を渡してください」と表示
   - 「準備OK」で探索フェーズへ

4. 探索フェーズ
   - 岩手県の地図を再表示
   - プレイヤー2が市区町村を選択
   - 1ターンにつき最大2市区町村を選択できる
   - 選択した市区町村を「探索済み」として視覚的に潰す（再選択不可）
   - nippleMunicipalities（2箇所）のうちどちらか一方だけではクリアにならない。両方とも見つけたらクリア

5. ハズレ / 片方発見
   - 片方だけ見つけた場合も探索継続（ゲーム終了しない）
   - 2回選択したら次ターンへ（当たり・ハズレ問わずカウント）
   - 探索済み市区町村はそのまま残す

6. 両方発見（クリア）
   - 2箇所とも見つけた時点でゲーム終了
   - 「乳首発見！」等の結果を表示

7. リセット
   - 「もう一度遵ぶ」でタイトル画面（phase: 'title'）へ戻る
   - stateを初期化


## 今後のTODO

### 乳首配置フェーズ（2箇所配置への変更）

- [x] nippleMunicipalityをnippleMunicipalities（配列）に変更する
- [x] 配置済みの市区町村をピンクのハイライトのまま残す
- [x] 既に配置済みの市区町村は選択・再配置できないようにする
- [x] 2箇所配置し終えたらhandoffフェーズへ進める

### 端末受け渡しフェーズ

- [x] phase stateを追加
- [x] 配置完了後に地図を隠す
- [x] 「次のプレイヤーに端末を渡してください」画面を作る
- [x] 「準備OK」ボタンを作る
- [x] 探索フェーズへ遷移する

### 探索フェーズ

- [x] 地図を表示
- [x] 市区町村を選択可能にする
- [x] 1ターン最大2回まで選択可能にする
- [x] 探索済み市区町村をstateで保持する
- [x] 探索済み市区町村を再選択不可にする（潰す）
- [x] 探索済み市区町村を地図上で視覚的に区別する
- [x] nippleMunicipalities（2箇所）を両方とも見つけたか判定（片方だけでは終了しない）
- [x] ハズレ・片方発見時のターン進行
- [x] 両方発見時にresultフェーズへ遷移

### ゲーム終了

- [x] 「乳首発見！」表示
- [x] リセット処理
- [x] 再プレイ可能にする

### MVP完成後、時間が余った場合のみ

- [ ] MUIで見た目を整える
- [ ] 市区町村コードではなく市区町村名を表示
- [x] タイトル画面
- [ ] 乳首の画像・アイコン・演出
- [ ] スマートフォン表示調整
- [ ] 無料環境へのデプロイ


## 想定ディレクトリ構成

MVPでは以下程度に留める。

```
src/
├── components/
│   └── IwateMap.tsx
│
├── features/
│   └── game/
│       ├── PlacementPhase.tsx
│       ├── HandoffPhase.tsx
│       ├── GuessingPhase.tsx
│       └── types.ts
│
├── constants/
│   └── map.ts
│
├── App.tsx
└── main.tsx

public/
└── N03-21_03_210101.json
```

ただし、現在のコードがこの構成になっていない場合、
一気に全面リファクタリングしないこと。

必要になったタイミングで小さく切り出す。

現状、上記コンポーネント分割はまだ行われておらず、
`App.tsx` に配置フェーズ・受け渡しフェーズのロジックがまとまっている。


## 設計方針

App.tsx:
ゲーム全体のphaseと主要stateを管理する。

IwateMap.tsx:
Mapboxの描画、市区町村クリック、ハイライトなど、
地図に関する処理を担当する。

PlacementPhase:
乳首を配置する。

HandoffPhase:
プレイヤー間の端末受け渡しを行う。

GuessingPhase:
市区町村を探索して乳首を探す。


## やらないこと

MVP完成までは以下を提案・実装しない。

- バックエンド
- DB
- WebSocket
- オンライン対戦
- 認証
- 状態永続化
- Redux等の状態管理ライブラリ
- Clean Architectureの導入
- Repository層
- UseCase層
- 過剰なカスタムHooks化
- 過剰な共通コンポーネント化
- 将来の全国対応を前提とした汎用化
- テストの大量作成
- パフォーマンス最適化
- 乳首を3箇所以上配置する仕様（2箇所固定）

必要になってから検討する。


## Copilotへの依頼

コード変更を提案する際は、まず現在のコードを確認すること。

既存実装を無視して大規模なコードを書き直さないこと。

基本的に、

1. 現在のコードを読む
2. 次に必要な最小単位を特定する
3. 必要最小限の変更を行う
4. 動作確認する
5. 次へ進む

という順序で開発する。

複数ステップを一度に実装せず、
ゲームが少しずつ動く状態を維持する。

現在の最優先目標は、

「岩手県を舞台に、乳首を配置して、別プレイヤーが探し当てるところまでゲームを最後まで遊べる状態にする」

こと。
